import React from 'react';

const Error = () => {
    return (
        <div style={{display: 'flex', justifyContent: 'center', textAlign: 'center', fontSize: '32px'}}>
            Ошибка. Такой страницы не существует:(
        </div>
    );
};

export default Error;